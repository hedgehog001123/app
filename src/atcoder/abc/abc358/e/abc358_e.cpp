#include <bits/stdc++.h>
using namespace std;

const long long mod = 998244353;

int main() {
    int K;
    cin >> K;
    vector<long long> C(26);
    for (int i = 0; i < 26; i++) cin >> C[i];

    long long Answer = 0;
    for (int i = 1; i <= K; i++) {
        // 文字数がi文字のときについて考える。
        
    }

    return 0;
}
