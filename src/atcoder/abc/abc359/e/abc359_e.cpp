#include <bits/stdc++.h>
using namespace std;

int main() {
    long long N;
    cin >> N;
    vector<long long> H(N);
    for (int i = 0; i < N; i++) cin >> H[i];
    
    long long left = 0, right = 0;
    for (int i = 0; i < N; i++) {
    }

    return 0;
}
